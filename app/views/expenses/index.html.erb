<h1>Living Expenses</h1>

<% @expenses.each do |expense| %>
  <%= render expense %>
<% end %>

<%= link_to "New", new_expense_path, remote: true, :'data-type' => :html, id: "new_expense_button" %>

<div id="modal"></div>
