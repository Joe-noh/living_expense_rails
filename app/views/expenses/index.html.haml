
= link_to "New", new_expense_path, class: "btn btn-primary"

%table.table.table-hover
  %thead
    %tr
      %th Purpose
      %th Responsible
      %th Payday
  %tbody
    - @expenses.each do |expense|
      = render expense
