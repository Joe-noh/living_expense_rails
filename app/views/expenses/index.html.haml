%table.table.table-hover
  %thead
    %tr
      %th Purpose
      %th.text-right Amount
      %th.text-right Payday
      %th.text-right Responsible
  %tbody
    - @expenses.each do |expense|
      = render expense
